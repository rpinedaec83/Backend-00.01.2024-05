<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head'); %>
</head>
<body class="container">

<header>
    <%- include('../partials/header'); %>
</header>

<main>
    <div class="jumbotron">
        <h1>Datos Ficticios de Usuario</h1>
        <p>Consulta datos ficticios de un usuario generado aleatoriamente:</p>
        <br>
        <% if (user) { %>
            <div>
                <img src="<%= user.picture.large %>" alt="Foto de <%= user.name.first %>">
                <h3><%= user.name.title %> <%= user.name.first %> <%= user.name.last %></h3>
                <p>Email: <%= user.email %></p>
                <p>Teléfono: <%= user.phone %></p>
                <p>País: <%= user.location.country %></p>
                <p>Dirección: <%= user.location.street.number %> <%= user.location.street.name %>, <%= user.location.city %>, <%= user.location.state %>, <%= user.location.postcode %></p>
            </div>
        <% } else { %>
            <p>No se encontraron datos del usuario.</p>
        <% } %>
    </div>

</main>

<footer>
    <%- include('../partials/footer'); %>
</footer>

</body>
</html>